export const getIsLoadingGames = (state) => state.app.isLoadingGames;

export const getAvailableGames = (state) => state.app.availableGames;

export const getCurrentGame = (state) => state.app.currentGame;
